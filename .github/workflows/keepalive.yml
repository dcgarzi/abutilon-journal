name: Supabase keep-alive

on:
  schedule:
    - cron: "17 9 * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -sf "https://hpdotarafccrwowlbyzw.supabase.co/rest/v1/keepalive?select=id&limit=1" \
            -H "apikey: sb_publishable_tnC1Nq4XIYffqGO8mOaJRQ_r40MBF5v" \
            -o /dev/null -w "HTTP %{http_code}\n"
